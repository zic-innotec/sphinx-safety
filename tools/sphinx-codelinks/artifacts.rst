Artifacts
=========

.. artifact:: Sphinx-CodeLinks needs.json file
   :id: ART_SCL_NEEDS_JSON

   A json file containing Needs objects.

   Often used to share requirements and co. in a technical way without
   the whole documentation project.

   Created during a :need:`TOOL_SPHINX` build or :need:`TOOL_SCL` cli build.